# @percentvibed/scanner-win32-x64

Platform package for the PercentVibed native scanner N-API addon.

When published and listed as an optional dependency, `@percentvibed/scanner` loads this `.node` addon on the matching platform.
